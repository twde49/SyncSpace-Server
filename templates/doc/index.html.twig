<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Documentation</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <h1>API Documentation</h1>
            <nav>
                <ul>
                    <li><a href="#get-started">Get Started</a></li>
                    <li><a href="#auth">Authentication</a></li>
                    <li><a href="#endpoints">Endpoints</a></li>
                </ul>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="content">
            <!-- Header -->
            <header>
                <h2>Welcome to the API Documentation</h2>
                <p>Your one-stop guide to integrating with our API.</p>
            </header>

            <!-- Sections -->
            <section id="get-started">
                <h3>Getting Started</h3>
                <p>Follow these steps to start using our API quickly.</p>
            </section>

            <section id="auth">
                <h3>Authentication</h3>
                <p>All endpoints require authentication. Include the following headers:</p>
                <pre class="json-block">{
    "Authorization": "Bearer YOUR_API_KEY"
}</pre>
            </section>

            <section id="endpoints">
                <h3>Endpoints</h3>

                <!-- GET Endpoint -->
                <div class="endpoint get">
                    <strong>GET</strong> /api/v1/resource
                    <p>Retrieve a list of resources.</p>
                </div>

                <!-- POST Endpoint -->
                <div class="endpoint post">
                    <strong>POST</strong> /api/v1/resource
                    <p>Create a new resource. Example body:</p>
                    <pre class="json-block">{
    "name": "Resource Name",
    "description": "Resource Description"
}</pre>
                </div>

                <!-- PUT Endpoint -->
                <div class="endpoint put">
                    <strong>PUT</strong> /api/v1/resource/:id
                    <p>Update an existing resource. Example body:</p>
                    <pre class="json-block">{
    "name": "Updated Resource Name",
    "description": "Updated Resource Description"
}</pre>
                </div>

                <!-- DELETE Endpoint -->
                <div class="endpoint delete">
                    <strong>DELETE</strong> /api/v1/resource/:id
                    <p>Delete a resource by its ID.</p>
                </div>
            </section>
        </main>
    </div>
</body>
</html>
